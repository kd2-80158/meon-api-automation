#To run API test suite use the following commands in the project's terminal:

1. Run Smoke Suite
./run-tests.sh smoke

2. Run Sanity Suite
./run-tests.sh sanity

3. Run Regression Suite (default TestNG XML)
./run-tests.sh regression

4. Get Help
./run-tests.sh help